# Optimized requirements for Vercel deployment
# Compatible with Python 3.12 (Vercel's default)
# Using minimum compatible versions to reduce size

# Core Data Analysis Libraries
pandas>=2.0.0,<2.1.0
numpy>=1.26.0,<1.27.0

# Statistical Analysis
scipy>=1.11.0,<1.12.0

# Machine Learning
scikit-learn>=1.3.0,<1.4.0

# Flask Backend
flask>=2.3.0,<2.4.0
flask-cors>=4.0.0,<4.1.0

# Note: If still exceeding 250MB, consider:
# 1. Upgrading to Vercel Pro plan
# 2. Using external data storage for CSV file
# 3. Splitting into multiple serverless functions

